---

---

<section class="py-32 bg-transparent relative">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 text-center relative z-10">
    <!-- Decorative line -->
    <div
      class="gsap-about-line w-16 h-0.5 bg-gradient-to-r from-amber-400 to-amber-600 mx-auto mb-12 origin-left scale-x-0"
    >
    </div>

    <h2
      class="gsap-about-title text-4xl md:text-5xl font-bold font-serif text-slate-900 dark:text-white mb-8 opacity-0"
    >
      Tentang Saya
    </h2>

    <div
      class="prose prose-lg dark:prose-invert mx-auto text-slate-600 dark:text-slate-400 leading-relaxed font-sans"
    >
      <p class="gsap-about-text mb-6 opacity-0">
        Halo! Saya
        <span
          class="text-amber-600 dark:text-amber-500 font-semibold gsap-highlight-name"
          >Grantly Sorongan</span
        >, seseorang yang memiliki ketertarikan besar pada dunia teknologi
        informasi sejak usia dini.
      </p>

      <p class="gsap-about-text mb-6 opacity-0">
        Ketertarikan tersebut mulai berkembang sejak usia 13 tahun, ketika saya
        membuat channel YouTube dan berhasil mengumpulkan lebih dari 2.000
        subscriber dan 500.000+ views dalam waktu kurang dari satu tahun. Dari
        pengalaman ini, saya belajar memahami pola audiens, strategi konten,
        serta bagaimana clickbait bekerja secara efektif dan etis.
      </p>

      <p class="gsap-about-text mb-6 opacity-0">
        Pada usia 16 tahun, saya mulai mendalami desain visual dan terbiasa
        menggunakan Adobe Photoshop untuk membuat berbagai kebutuhan grafis.
        Pengalaman ini membentuk kepekaan saya terhadap estetika dan detail
        visual.
      </p>

      <p class="gsap-about-text opacity-0">
        Memasuki usia 19 tahun, saya mulai fokus pada dunia pemrograman dan
        pengembangan web hingga saat ini. Saya menikmati proses menggabungkan
        desain yang rapi dengan sistem yang solid untuk menciptakan solusi
        digital yang fungsional, efisien, dan berdampak.
      </p>
    </div>
  </div>
</section>

<script>
  import { gsap } from "gsap";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(ScrollTrigger);

  document.addEventListener("DOMContentLoaded", () => {
    const aboutTl = gsap.timeline({
      scrollTrigger: {
        trigger: "section:has(.gsap-about-title)",
        start: "top 75%",
      },
      defaults: { ease: "power3.out" },
    });

    // Line draws in
    aboutTl
      .to(".gsap-about-line", {
        scaleX: 1,
        duration: 0.8,
        ease: "power2.inOut",
      })
      // Title fades up
      .to(
        ".gsap-about-title",
        {
          opacity: 1,
          y: 0,
          duration: 1,
        },
        "-=0.3",
      )
      // Text paragraphs fade in
      .to(
        ".gsap-about-text",
        {
          opacity: 1,
          y: 0,
          duration: 0.8,
          stagger: 0.2,
        },
        "-=0.5",
      )
      // Name gets a golden pulse
      .fromTo(
        ".gsap-highlight-name",
        {
          textShadow: "0 0 0px rgba(245, 158, 11, 0)",
        },
        {
          textShadow: "0 0 20px rgba(245, 158, 11, 0.4)",
          duration: 0.6,
          yoyo: true,
          repeat: 1,
        },
        "-=0.3",
      );

    // Set initial states
    gsap.set(".gsap-about-title", { y: 30 });
    gsap.set(".gsap-about-text", { y: 20 });
  });
</script>
